# TODO
- [x] Make stock search errors visible in UI (stop swallowing errors in `SearchCommand.tsx`).
- [x] Add server-side debug logging for Finhub requests (status + response body) in `lib/actions/finnhub.actions.ts` for both `/search` and `/stock/profile2` paths.
- [x] Re-test search for: (a) empty query / popular stocks, (b) typed query / search endpoint.

